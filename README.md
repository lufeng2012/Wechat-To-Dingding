# Wechat-To-Dingding
wechat transfer dingding
#python3.9 环境要输入本地路径如我本地
I:\tool\python3.9\python.exe
#python环境中以来wxauto库，编译环境务必要将python环境和路径下的python3.9环境一致，如果
#是visual studio则，选中启动右侧的，新建环境后，将I:\tool\python3.9\python.exe添加进去，
#之后右侧“Python 环境”中选择刚才新建的环境，之后点开“概述”后，看到 在PowerShell中打开命令。
#在该命令下执行pip install . 安装命令。

如果在visual studio，工具中打开PowerShell 则无效

